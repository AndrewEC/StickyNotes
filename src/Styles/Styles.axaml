<Styles xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style Selector="Button.MenuButton">
        <Setter Property="CornerRadius" Value="0" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="10" />
    </Style>

    <Style Selector="Button.MenuButtonPink">
        <Setter Property="Background" Value="#FFCCFF" />

        <Style Selector="^:pointerover /template/ ContentPresenter">
            <Setter Property="Background" Value="#FFE6FF" />
        </Style>
    </Style>

    <Style Selector="Button.MenuButtonBlue">
        <Setter Property="Background" Value="#99bbff" />

        <Style Selector="^:pointerover /template/ ContentPresenter">
            <Setter Property="Background" Value="#ccddff" />
        </Style>
    </Style>

    <Style Selector="Button.MenuButtonGreen">
        <Setter Property="Background" Value="#99ff99" />

        <Style Selector="^:pointerover /template/ ContentPresenter">
            <Setter Property="Background" Value="#ccffcc" />
        </Style>
    </Style>

    <Style Selector="TextBox.MainInput">
        <Setter Property="CornerRadius" Value="0" />
        <Setter Property="BorderThickness" Value="0" />

        <Style Selector="^:focus">
            <Setter Property="CornerRadius" Value="0" />
            <Setter Property="BorderThickness" Value="0" />

            <Style Selector="^ /template/ Border#PART_BorderElement">
                <Setter Property="BorderThickness" Value="0" />
            </Style>
        </Style>
    </Style>

    <Style Selector="TextBox.MainInputPink">
        <Setter Property="Background" Value="#FFE6FF" />
        <Setter Property="SelectionBrush" Value="#FFA8B9FF" />

        <Style Selector="^:pointerover">
            <Style Selector="^ /template/ Border#PART_BorderElement">
                <Setter Property="Background" Value="#FFE6FF" />
            </Style>
        </Style>

        <Style Selector="^:focus">
            <Style Selector="^ /template/ Border#PART_BorderElement">
                <Setter Property="Background" Value="#FFE6FF" />
            </Style>
        </Style>
    </Style>

    <Style Selector="TextBox.MainInputBlue">
        <Setter Property="Background" Value="#ccddff" />
        <Setter Property="SelectionBrush" Value="#99bbff" />

        <Style Selector="^:pointerover">
            <Style Selector="^ /template/ Border#PART_BorderElement">
                <Setter Property="Background" Value="#ccddff" />
            </Style>
        </Style>

        <Style Selector="^:focus">
            <Style Selector="^ /template/ Border#PART_BorderElement">
                <Setter Property="Background" Value="#ccddff" />
            </Style>
        </Style>
    </Style>

    <Style Selector="TextBox.MainInputGreen">
        <Setter Property="Background" Value="#ccffcc" />
        <Setter Property="SelectionBrush" Value="#99ff99" />

        <Style Selector="^:pointerover">
            <Style Selector="^ /template/ Border#PART_BorderElement">
                <Setter Property="Background" Value="#ccffcc" />
            </Style>
        </Style>

        <Style Selector="^:focus">
            <Style Selector="^ /template/ Border#PART_BorderElement">
                <Setter Property="Background" Value="#ccffcc" />
            </Style>
        </Style>
    </Style>

    <Style Selector="StackPanel.MenuBar">
        <Setter Property="Orientation" Value="Horizontal" />
    </Style>

    <Style Selector="StackPanel.MenuBarPink">
        <Setter Property="Background" Value="#FFCCFF" />
    </Style>

    <Style Selector="StackPanel.MenuBarBlue">
        <Setter Property="Background" Value="#99bbff" />
    </Style>

    <Style Selector="StackPanel.MenuBarGreen">
        <Setter Property="Background" Value="#99ff99" />
    </Style>

    <Style Selector="StackPanel.ColourOptions">
        <Setter Property="Orientation" Value="Horizontal" />
        <Setter Property="Margin" Value="10" />
    </Style>

</Styles>